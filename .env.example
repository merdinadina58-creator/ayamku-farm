# PostgreSQL connection string from Vercel Postgres (or any Postgres provider).
# Get this from: Vercel Dashboard > Storage > your Postgres db > .env.local tab
# Example: postgres://default:password@host:port/dbname?sslmode=require
DATABASE_URL=
